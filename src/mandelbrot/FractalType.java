package mandelbrot;

/**
 * Enumeration object that contains the two different fractals that this program can draw. This class is used to allow for easy
 * and understandable differentiation between the fractals. Mainly used when dealing with FractalSets as it is the only
 * descriptor of what fractal is contained within each FractalSet.*/

public enum FractalType {MANDELBROT, JULIA}